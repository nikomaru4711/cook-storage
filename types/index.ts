export type Recipe = {
    id: string;
    name: string;
    url?: string;
    ingredients: string;
    created_at: string;
}

export type ingredients = {
    id: string;
    name: string;
    created_at: string;
}